# Default fallback bundle (loaded when locale-specific bundle has no key).
# Spring Boot ResourceBundleMessageSource with defaultLocale=ru reads this only if a key
# is missing in messages_ru.properties — kept intentionally empty so that gaps are visible.
# Comments here MUST be in English (file uses ISO-8859-1 by default — Spring overrides to UTF-8 via I18nConfig).
