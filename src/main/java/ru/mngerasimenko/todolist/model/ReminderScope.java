package ru.mngerasimenko.todolist.model;

/** Кому адресовано напоминание о сроке задачи. */
public enum ReminderScope {
    /** Только автору задачи. Значение по умолчанию и единственно возможное для приватной задачи. */
    SELF,
    /** Всем участникам списка, включая автора. */
    ALL
}
