# target/ intentionally NOT excluded — pre-built JAR is copied into Docker image
# Build the JAR first with: mvn clean package -DskipTests
.mvn/
.mvnw
.mvnw.cmd

# Исключаем IDE-файлы
.idea/
.vscode/
*.iml

# Исключаем системные файлы
.git/
.gitignore
.gitattributes

# Исключаем логи
*.log
logs/

# Исключаем временные файлы
*.tmp
*.swp
*.swo
.DS_Store

# Исключаем ненужные артефакты Maven
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup